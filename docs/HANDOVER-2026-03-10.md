# NC7 Studio3D — Handover (2026-03-10 session)

**Repo path:** `/Users/nc7foamart/NC7Studio3D`  
**Cursor workspace may show:** `RVE-VectorLinker` (same machine, different folder name)  
**Last stable git tag:** `savepoint-2026-03-10` (`4c42396`)  
**DevFoam reference commit:** `e3fbf25`  
**Current branch:** `main` — **uncommitted work** (see §2)

---

## 1. Project in one paragraph

Web CAM for NC7 **hot-wire foam cutter** (Vite + React + Three.js). Flow: **Model → Toolpath → G-code → Simulate**. Method 1 (left-only wire path) is v1 scope per `docs/CONCEPT.md`. DevFoam G-code in `Example/` is the quality reference (`docs/DevFoamLogic.md`).

---

## 2. Uncommitted changes (this session — NOT on git yet)

Run `git status` — expected modified files:

| File | What changed |
| :--- | :--- |
| `src/lib/silhouette.js` | **Collimated shadow** UV raster + Profile accuracy (1–10); envelope fallback |
| `src/lib/toolpath.js` | `geometryForToolpathSlicing`, `silhouetteOptsFromStock`, shadow wired in |
| `src/lib/settle.js` | `ensureGeometryOnFloor()` — mutates geometry to Y_min = 0 |
| `src/context/AppState.jsx` | profileAccuracy deps; auto floor on `recomputeToolpath`; cutJob silhouette opts |
| `src/components/Viewer3D.jsx` | Left **wire path** (orange) on MP; grey raw silhouette; red Y-axis cylinder; toolpath overlays |
| `src/components/ToolpathParametersForm.jsx` | Profile accuracy slider |
| `src/pages/ToolpathPage.jsx` | silhouette + wire pt counts; stats bar `wire N pts` |
| `src/lib/cutJob.js` | Passes `silhouetteOpts` |
| `src/index.css` | Profile accuracy range; status bar above stats on toolpath |

**Recommended before quota ends:**

```bash
cd /Users/nc7foamart/NC7Studio3D
git add -A
git commit -m "Toolpath: shadow silhouette, profile accuracy, wire path preview, floor settle"
git tag savepoint-2026-03-10-session2
```

---

## 3. How to run

```bash
cd /Users/nc7foamart/NC7Studio3D
npm install
npm run dev
```

- Local: `http://localhost:5173`
- Tailscale (if configured): see terminal output / `vite.config.js` `allowedHosts`

```bash
npm run build   # verify compile
```

Default model: `Example/KnightChessNoHair.stl`

---

## 4. What works now (session end)

| Feature | Status |
| :--- | :--- |
| Silhouette algorithm | **Shadow raster** (DevFoam-style) + Profile accuracy 1–10; fallback envelope |
| Knight @ θ=0, acc=5 | ~128 silhouette pts (DevFoam ref ~126) |
| Toolpath UI | Gear setup (⚙), W/T/H/LO/BO/kerf, show bbox toggle |
| 3D Toolpath preview | **Orange** = left wire path (kerf+clamp); **grey** = raw silhouette; **red cylinder** = Y-axis from Y=0, height 2× model |
| Model on floor | `ensureGeometryOnFloor()` on each `recomputeToolpath` |
| Model page gizmo | Disabled on Toolpath (`readOnly`) |
| Session / project | IndexedDB + `.nc7` project files |
| DevFoam docs | `docs/DevFoamLogic.md`, `Example/StackedCut*.nc` |

---

## 5. Known issues / verify after handover

1. **Floor settle timing** — geometry is mutated on first toolpath recompute (also runs while on Model page via global AppState effect). Confirm this is acceptable for editing workflow.
2. **Y-axis red cylinder** — intentional machine rotary axis viz; pierces model at X=Z=0 (block centre). May need offset or toggle.
3. **2D PathPreviewCanvas** — disabled (`SHOW_2D_TOOLPATH_PREVIEW = false` in `ToolpathPage.jsx`).
4. **Silhouette-edge attempt** — reverted; kept at `src/lib/checkpoints/silhouette-edges-v1.js` (bad on mobile).
5. **G-code vs DevFoam** — not yet golden-compared to `Example/StackedCut.nc`.
6. **Method 2 Left+Right** — v2 only; not implemented.
7. **User screenshot (pre-fix)** — model half below grid; fix is `ensureGeometryOnFloor` — **needs visual confirm** after hard refresh.

---

## 6. Architecture quick map

```
src/lib/silhouette.js     ← shadow raster, extractLeft/FullSilhouette
src/lib/toolpath.js       ← cutting plane, buildSectionProfile, MP shift
src/lib/wirePath.js       ← kerf, clamp to stock, top→bottom order
src/lib/cutJob.js         ← N cuts, half-span 0–180°
src/lib/gcode.js          ← Method 1 G93 export
src/context/AppState.jsx  ← state, session, recomputeToolpath
src/components/Viewer3D.jsx ← 3D + toolpath overlays
```

**Source of truth:** `docs/CONCEPT.md`  
**DevFoam mapping:** `docs/DevFoamLogic.md`

---

## 7. Silhouette checkpoints

| File | Use |
| :--- | :--- |
| `src/lib/checkpoints/silhouette-envelope-v2.js` | Pre-shadow envelope baseline (savepoint tag) |
| `src/lib/checkpoints/silhouette-edges-v1.js` | Silhouette-edge experiment — do not enable without testing |

Restore envelope:

```bash
cp src/lib/checkpoints/silhouette-envelope-v2.js src/lib/silhouette.js
```

---

## 8. Priority next work (ordered)

1. **Visual QA** — Knight on Toolpath: model base on grid Y=0, orange wire hugs surface, acc 1↔10 changes pt count.
2. **G-code golden compare** — export NC7 vs `Example/StackedCut.nc` (normalize coords / point count).
3. **Commit + tag** — see §2.
4. **Optional:** boundary walk on shadow grid (better concavity, same pt budget).
5. **v2:** Method 2 left+right continuous (`StackedCut2_CutLeft-Right.nc`).
6. **G94 first move + Y729 lift** per DevFoamLogic §8.
7. **Web Worker** for heavy silhouette on large STL.

---

## 9. Key parameters (Toolpath setup ⚙)

| Param | Default | Notes |
| :--- | :--- | :--- |
| Profile accuracy | 5 | 1–10; grid density `vBins = 50 + acc×16` |
| Kerf | 2 mm | Shifts wire left (u − kerf/2) |
| Stock W/T/H | auto from bbox | User can override |
| Rotation N | 16 | Half-span → 8 cuts, step 360/N |
| showModelBBox | true | Yellow bbox toggle |

---

## 10. User preferences (important)

- User types English quickly; **reports in Thai + English technical terms**.
- Minimize scope; match existing code style.
- Do not re-enable silhouette-edge without mobile testing.

---

## 11. Related docs

- `docs/HANDOVER.md` — older Phase 1 handover (partially stale)
- `docs/SAVEPOINT-2026-03-10.md` — pre-shadow save point notes
- `Example/README.md` — G-code sample descriptions

---

*Generated for session handover — quota depletion. Next agent: start with §5 visual QA + §2 commit.*
