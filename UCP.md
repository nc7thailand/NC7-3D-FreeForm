# UCP — User Communication Protocol

> โปรโตคอลการสื่อสารระหว่าง **Bank (User)** กับ **AI Assistant** ในโปรเจกต์นี้
> Version: `1.0` · เริ่มใช้: 9 กันยายน 2026 · Owner: Bank · จัดทำโดย: Cline

---

## 1. วัตถุประสงค์ (Purpose)

กำหนด "โหมดการทำงาน" ของผู้ช่วยให้ชัดเจน เพื่อให้ Bank ควบคุมได้เองว่าตอนนี้ต้องการ
**คุย/คิด/บรีนสตรอม** หรือ **ลงมือเขียนโค้ดจริง** — ลดความเข้าใจผิดที่ผู้ช่วยเผลอแก้ไฟล์
หรือรันคำสั่งในขณะที่ยังแค่ต้องการคุยไอเดียอยู่

---

## 2. คำสั่งควบคุม (Control Commands)

| คำสั่ง | ชื่อโหมด | ความหมาย |
|---|---|---|
| `/Chat` | 🟦 **Chat Mode** | เข้าโหมดคุย — ห้ามเขียนโค้ด/แก้ไฟล์/รันคำสั่งที่เปลี่ยนสถานะ |
| `/Run` | 🟩 **Run Mode** | ออกจากโหมดคุย — ทำงานจริงได้เต็มที่ (เขียนโค้ด, แก้ไฟล์, รันคำสั่ง, ทดสอบ) |

### กติกาการอ่านคำสั่ง

1. ต้องเป็นข้อความที่ **ขึ้นต้น** ด้วย `/Chat` หรือ `/Run` แบบตรงตัว (case-insensitive: `/chat`, `/RUN` ก็ได้)
2. ถ้าคำสั่งอยู่ใน **code block** หรืออัญประกาศ (`"..."`) → ถือเป็นข้อความธรรมดา ไม่ใช่คำสั่ง
3. ถ้าส่งทั้งสองคำสั่งมาในข้อความเดียว → **คำสั่งสุดท้ายชนะ**
4. โหมดเป็นแบบ **sticky** — คงอยู่จนกว่าจะสลับด้วยคำสั่งอีกตัว (ไม่มี timeout, ไม่รีเซ็ตเอง)
5. คำสั่งสลับโหมด **ไม่ต้องมีข้อความอื่นกำกับ** ก็ได้ (ส่ง `/Chat` เดี่ยวๆ = สลับโหมด)

---

## 3. ค่าเริ่มต้น (Default Mode)

- เริ่มต้นเป็น **🟩 Run Mode** เสมอ (พฤติกรรมปกติของผู้ช่วย)
- จะเปลี่ยนเป็น Chat Mode ก็ต่อเมื่อ Bank ส่ง `/Chat` เท่านั้น
- การเปลี่ยนโหมดไม่กระทบประวัติการคุย — บริบทเดิมยังต่อเนื่องได้

---

## 4. พฤติกรรมของผู้ช่วยในแต่ละโหมด

### 🟦 Chat Mode — คุย / บรีนสตรอม

**ทำได้**
- คุย ตอบคำถาม วิเคราะห์ เสนอไอเดีย ตั้งสมมติฐาน ถกข้อดี/ข้อเสีย
- เสนอทางเลือกสถาปัตยกรรม แผนงาน ลำดับการทำ เปรียบเทียบเครื่องมือ
- **อ่านไฟล์/ค้นหาโค้ดแบบ read-only** เพื่อให้คำตอบอ้างอิงของจริงได้ (ไม่แก้ไฟล์)
- ยกตัวอย่างโค้ดสั้นๆ **ในคำตอบ** ได้ ถ้าจำเป็นต่อการอธิบาย — แต่ไม่เขียนลงไฟล์

**ห้าม**
- แก้ไข / สร้าง / ลบไฟล์ในโปรเจกต์
- รันคำสั่งที่เปลี่ยนสถานะเครื่อง (build, install, start server, docker, git commit, ฯลฯ)
- ลงมือ implement งาน แม้ Bank จะเล่า requirement ละเอียดแค่ไหน

**ถ้าจำเป็นต้องแก้โค้ด/รันคำสั่งจริง** → ผู้ช่วยต้อง **ขอสลับโหมดก่อน** เสมอ เช่น
> "งานนี้ต้องแก้โค้ดจริง — ขออนุญาตสลับเป็น Run Mode นะครับ"

### 🟩 Run Mode — ลงมือทำงาน

- ทำได้เต็มที่: อ่าน/เขียน/แก้ไฟล์, รันคำสั่ง, ติดตั้ง dependency, build, test, ตรวจผลลัพธ์
- ยังยึดหลักเดิม: อธิบายแผนก่อนลงมือ, ขอความชัดเจนเมื่อ requirement กำกวม
- ก่อน **ลบไฟล์/แก้ของสำคัญ/รันคำสั่งที่ย้อนกลับยาก** → ยืนยันกับ Bank ก่อนเสมอ
- งานที่ทำเสร็จแล้วควรสรุปผลลัพธ์ (+ วิธีตรวจสอบ) ให้ Bank ตามที่ผ่านมา

---

## 5. การประกาศสลับโหมด (Mode Handshake)

เมื่อสลับโหมด ผู้ช่วยจะประกาศสั้นๆ **หนึ่งบรรทัด** เพื่อให้ทั้งสองฝ่ายเห็นตรงกัน
(ประกาศเฉพาะตอนสลับ ไม่ต้องประกาศซ้ำทุกข้อความ):

- เข้า Chat Mode → `🟦 Chat Mode ON — คุย/บรีนสตรอม (ยังไม่แก้โค้ด)`
- เข้า Run Mode → `🟩 Run Mode ON — ลงมือทำงานได้`

---

## 6. ตัวอย่างการใช้งาน

```text
Bank : /Chat
AI   : 🟦 Chat Mode ON — คุย/บรีนสตรอม (ยังไม่แก้โค้ด)
       เอาละ อยากคุยเรื่องอะไรครับ?
Bank : ทำ relay chat กับ Open WebUI ดีไหม
AI   : (คุย วิเคราะห์ เปรียบเทียบ ไม่แก้ไฟล์)

Bank : /Run
AI   : 🟩 Run Mode ON — ลงมือทำงานได้
       โอเคครับ ผมจะเริ่มจากการตรวจ environment ก่อน…
```

ตัวอย่างที่ถือเป็น "ข้อความธรรมดา" ไม่ใช่คำสั่ง:
```text
Bank : ช่วยอธิบายว่า `/Chat` กับ `/Run` ต่างกันยังไง
```

---

## 7. ข้อควรรู้ / ข้อยกเว้น

- UCP เป็นชั้นที่ผู้ใช้กำหนดเอง ทำงานคู่กับ **Plan/Act ของ IDE** ได้:
  Chat Mode ≈ วางแผน/วิเคราะห์ (ห้ามแก้ไฟล์) · Run Mode ≈ ลงมือทำ
- **ความปลอดภัยมาก่อนเสมอ** — ต่อให้อยู่ใน Run Mode ผู้ช่วยก็ยังไม่ทำสิ่งที่ทำลายข้อมูล
  หรือย้อนกลับไม่ได้ โดยไม่ยืนยันก่อน
- ถ้าใน Chat Mode มี "งานค้างที่ต้องเขียนโค้ด" ผู้ช่วยจะจดเป็น **TODO ในบทสนทนา**
  และรอคำสั่ง `/Run` แล้วค่อยดำเนินการ
- ถ้าข้อความกำกวมว่าผู้ใช้ต้องการคุยหรือลงมือ → ผู้ช่วยจะ **ถามสั้นๆ** ว่าให้อยู่โหมดไหน

---

## 8. ประวัติการแก้ไข

| Version | วันที่ | รายละเอียด |
|---|---|---|
| 1.0 | 2026-09-09 | กำหนดคำสั่ง `/Chat` และ `/Run` ครั้งแรก (โดย Cline) |
